<p>
  <strong>Name:</strong>
  <%= @artist.name %>
</p>

<p>
  <strong>nationality:</strong>
  <%= @artist.nationality %>
</p>

<img src="<%= @artist.photo_url %>">

<p><%= link_to "edit this artist", edit_artist_path(@artist) %></p>
<p><%= link_to 'delete this artist', artist_path(@artist), method: :delete, data:{confirm: "Are you sure you want to delete this artist?"} %></p>

<p><%= link_to 'Back', artists_path %></p>
